import { Stack } from "@mui/material";
import { styled } from "@mui/material/styles";

export const AboutSectionStyled = styled(Stack)({
  flexDirection: "row",
  padding: "40px 120px",
  gap: "120px",
});

export const AboutItemStyled = styled(Stack)({
  width: "70%",
  gap: "20px",
});

export const ExperienceItemStyled = styled(Stack)({
  width: "30%",
  gap: "20px",
});
